Gradient duotone "glassy" icon — deep brand-blue base + translucent mint→cyan overlay.

```jsx
<Icon name="target" size={32} />
<Icon name="globe" />
```

Names: `target` · `chat` · `globe` · `timer` · `clock` · `calendar` · `monitor` · `users`.
Use for hero event-meta rows (calendar / clock / monitor / users) and feature cards
(target / chat / globe / timer). Replaces the old emoji icons. Gradient stops read the
brand blue tokens, so icons stay on-palette automatically.
