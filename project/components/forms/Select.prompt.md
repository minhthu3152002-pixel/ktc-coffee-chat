Labelled dropdown matching Input, with a custom SVG chevron.

```jsx
<Select label="Kinh nghiệm" required options={['Fresher (dưới 1 năm)', '1–2 năm', '3–5 năm', '5+ năm']} />
```

Pass `options` as a string array, or supply `<option>` children. `placeholder` sets the disabled default ("Chọn").
