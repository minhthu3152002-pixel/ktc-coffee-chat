Accordion FAQ row.

```jsx
<FaqItem q="Buổi này có phí không?" a="Miễn phí hoàn toàn." />
<FaqItem q="Không biết tiếng Hàn có tham gia được không?" a="Hoàn toàn được. KTC hỗ trợ phiên dịch Việt–Hàn real-time." defaultOpen />
```

Stack in a `flex column` with `gap: 10px`. Self-manages open/closed state.
